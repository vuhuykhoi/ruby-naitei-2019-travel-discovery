<%= form_for(current_user.reactions.build, remote: true) do |f| %>
  <div>
    <%= hidden_field_tag :user_id, current_user.id %>
    <%= hidden_field_tag :post_id, post_id %>
    <%= hidden_field_tag :reaction_type_id, 1 %>
  </div>
  <%= button_tag(type: "submit", class: "btn float-left") do %>
    <i class="far fa-thumbs-up"></i> <%= t("button.like") %>
  <% end %>
<% end %>
