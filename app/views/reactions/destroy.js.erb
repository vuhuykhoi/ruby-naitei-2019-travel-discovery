$("#reaction_form-<%=@post_id %>").html("<%= escape_javascript(render partial: 'shared/reaction/like',locals: { post_id: @post_id }) %>");
<% num_of_likes =  Reaction.get_like(Settings.reaction_type.like,@post_id).count %>;
$("#num_of_likes-<%= @post_id %>").html("<%= I18n.t(:'label.like', count: num_of_likes)%>");
